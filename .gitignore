# Dependencies — never committed, install via npm install
node_modules/

# Build output
dist/
.output/

# Local DB (SQLite, machine-specific)
local.db
*.db

# Environment secrets — each dev has their own .env.local
.env.local
.env.*.local

# TanStack Start / Vite internals (auto-generated)
.tanstack/

# Supabase temp/auth cache
supabase/.temp/

# OS
.DS_Store
Thumbs.db
